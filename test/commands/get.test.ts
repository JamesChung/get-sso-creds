import { expect, test } from '@oclif/test'

describe('get', () => {
  // test
  // .stdout()
  // .command(['get'])
  // .it('runs get', ctx => {
  //   expect(ctx.stdout).to.contain('Profile:')
  // })

  // test
  // .stdout()
  // .command(['get', '--profile', 'default'])
  // .it('runs get --profile default', ctx => {
  //   expect(ctx.stdout).to.contain('Profile:')
  // })
})
