import { expect, test } from '@oclif/test'

describe('list', () => {
  // test
  // .stdout()
  // .command(['list'])
  // .it('runs list', ctx => {
  //   expect(ctx.stdout).to.contain('Select a profile')
  // })
})
