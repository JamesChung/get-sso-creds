import { expect, test } from '@oclif/test'

describe('signin', () => {
  // test
  // .stdout()
  // .command(['signin'])
  // .it('runs list', ctx => {
  //   expect(ctx.stdout).to.contain('')
  // })
})
